# Benchmark4Chip 负载清单

本文件是 **30 项 benchmark 的权威列表** 与 **硬件考察维度** 说明。  
工程结构、运行方式、指标方法论见根目录 [README.md](./README.md)。

---

## 一、AI 训练 Benchmark

LLaMA-7B 稠密预训练（通用 LLM 稠密算力 + 并行通信）
BF16/FP16 张量算力利用率、片上 SRAM 缓存命中率、激活重计算显存开销、多卡 TP/PP 集体通信带宽、NOC 片上网络吞吐、显存带宽饱和能力
MoE 混合专家模型训练（稀疏动态算力）
动态不规则张量寻址、路由算子硬件加速、动态显存分配效率、专家负载均衡调度能力、稀疏矩阵计算单元效能、跨专家数据搬运带宽
Stable Diffusion UNet 完整训练（卷积 + 循环迭代细碎算子）
小尺寸卷积算子执行效率、算子调度流水线开销、残差分支数据流复用、大量小 Tensor 频繁访存、低精度数值稳定性
ViT-MAE 视觉自监督训练（纯视觉 Transformer）
长序列注意力矩阵乘算力、Patch 张量重排开销、大维度张量片内搬运、归一化层硬件加速、高带宽 DRAM 吞吐
Megatron 超大模型张量并行模拟（多芯片互联）
MCM 多芯片封装高速接口带宽、AllReduce/AllGather 规约延迟、多 die NOC 拓扑效率、参数分片流水并行能力、跨芯片同步开销
FP8 QAT 量化感知训练（混合精度硬件适配）
FP8 训练原生算力、量化 / 伪量化算子硬加速、浮点与低精度数据流切换开销、量化误差硬件数值保真度、混合精度存储带宽利用率

## 二、AI 推理 Benchmark

LLaMA-7B 增量解码推理（KV 缓存复用、自注意力增量计算）
片上高速缓存对 KV Cache 驻留能力、动态序列长度算力弹性、解码时延 / 首包响应延迟、批量推理调度单元、权重带宽瓶颈
Llava 多模态图文联合推理（视觉编码器 + LLM 异构流水线）
异构算子流水线并行执行能力、多模型数据流无缝切换、特征投影张量传输开销、多任务资源隔离机制
SD UNet 迭代采样推理（多步前向细碎卷积算子负载）
循环多次推理算子启动开销、卷积脉动阵列利用率、中间张量片上缓存复用、功耗与时延折中表现
Whisper 流式长音频推理（时序滑动窗口超长序列计算）
滑动窗口张量分段处理能力、超长上下文显存占用控制、时序类 Attention 硬件延迟、流式数据 IO 吞吐
INT8/FP8 量化推理（低精度算子与数值稳定性验证）
INT8/FP8 推理算力峰值、量化反量化硬件开销、零值点偏移运算单元、低精度下精度误差容错能力
BERT 批量向量编码推理（小 Transformer 高并发吞吐压力）
批量并行计算单元利用率、短序列算子调度效率、高并发下算力溢出阈值、片上缓存批量数据吞吐

## 三、端侧 AI Benchmark（NPU / 嵌入式 AI 芯片 / MCU）

YOLOv8-Nano 实时检测推理（轻量化卷积 + 后处理 NPU 算子）
深度可分离卷积硬件效率、NMS / 置信度筛选后处理硬加速、低功耗模式算力输出、片内小容量 SRAM 利用率
MobileNetV3 分类推理（深度可分离卷积低带宽负载）
卷积数据流寄存器复用、访存带宽节约架构、极低功耗下有效算力、MCU 级窄内存接口适配能力
DistilBERT 端侧小 LLM 推理（片上存储极限约束测试）
权重压缩后片上 Flash 加载速度、极小缓存下 Attention 分块计算、内存带宽受限场景算力衰减率
Qwen-1.8B INT4 量化离线推理（权重压缩与带宽瓶颈）
INT4 解压硬件单元、4bit 权重访存带宽压力、片上缓存分块加载策略、低比特数值计算稳定性
MediaPipe 多任务串行推理（多模型调度开销）
模型快速切换上下文保存 / 恢复开销、流水线任务硬件调度器、多算子串行流水吞吐、功耗动态调节能力
轻量化 MAE 视觉提取推理（端侧视觉特征低功耗负载）
低帧率算力节流控制、图像 Patch 预处理硬件加速、休眠 / 运行功耗切换表现

## 四、云端 AI Benchmark（数据中心大规模集群）

大模型分布式张量并行推理（MCM 多芯片互联与规约通信）
多 die 高速互联带宽、推理阶段 AllReduce 规约延迟、片间 NOC 拥塞控制、多芯片负载均衡调度
MoE 稀疏专家云端批量推理（动态路由负载不均衡访存）
稀疏 Token 动态分发硬件单元、专家核算力动态调度、不规则访存带宽冲突规避、批量任务负载打散能力
多模型混部租户压测（算力调度与资源隔离开销）
硬件资源虚拟化隔离、算力切片抢占调度、缓存分区保护、多任务 QoS 时延保障能力
CLIP 海量图片分布式特征抽取（IO + 算力满负载跑批）
PCIe / 主机 DMA 带宽上限、批量图像预处理硬加速、算力持续满载稳定性、DRAM 持续读写带宽
Embedding 高并发向量推理（网络吞吐与批量调度压力）
超大 Batch 并行算力、向量归一化硬件加速、网卡直连张量传输开销、并发请求队列硬件调度器
训推一体 FP8 全链路负载（混合精度端到端硬件适配）
FP8 训推统一数据通路、训练量化→推理加载无缝流转、混合精度存储带宽综合利用率

## 五、科学计算 HPC Benchmark

OpenFOAM CFD 流体求解（双精度稠密线性代数算力）
FP64 双精度峰值算力、稠密矩阵乘阵列效率、迭代求解器访存局部性、大规模方程组存储带宽
LAMMPS 分子动力学仿真（不规则随机内存访问）
随机散列访存架构效率、粒子力场计算并行度、非连续地址访问缓存命中率、大数组寻址延迟
NPB 并行 HPC 基准（MPI 通信与浮点算力匹配度）
多进程 MPI 通信接口带宽、计算 / 通信重叠执行能力、浮点运算单元持续效率、并行同步开销
3D-FFT 大规模傅里叶变换（内存带宽极限压榨）
多维张量转置硬件开销、跨步访存带宽利用率、FFT 蝶形运算单元加速、大尺寸数组缓存分块能力
FEA 有限元稀疏矩阵求解（SpMV 稀疏张量运算）
SpMV 稀疏矩阵乘法专用单元、不规则稀疏存储遍历效率、非零元素索引硬件寻址、带宽受限下算力衰减
大气环流气候模式迭代计算（大数组持续访存压力）
超大型数组流式读写带宽、长时间步迭代算力稳定性、循环变量寄存器复用、大容量 DRAM 吞吐上限
